from __future__ import annotations

from datetime import date
from pathlib import Path

from docx import Document
from docx.enum.text import WD_ALIGN_PARAGRAPH


def add_title(doc: Document, text: str) -> None:
    p = doc.add_paragraph()
    p.alignment = WD_ALIGN_PARAGRAPH.CENTER
    r = p.add_run(text)
    r.bold = True


def add_h1(doc: Document, text: str) -> None:
    doc.add_heading(text, level=1)


def add_h2(doc: Document, text: str) -> None:
    doc.add_heading(text, level=2)


def add_p(doc: Document, text: str) -> None:
    doc.add_paragraph(text)


def add_bullets(doc: Document, items: list[str]) -> None:
    for item in items:
        doc.add_paragraph(item, style="List Bullet")


def build_doc() -> Document:
    doc = Document()

    add_title(doc, "Интерактивная 3D-химия (VR/AR платформа): описание проекта")
    add_p(doc, f"Дата: {date.today().strftime('%d.%m.%Y')}")
    doc.add_paragraph()

    add_h1(doc, "Актуальность проекта")
    add_p(
        doc,
        "Проект направлен на помощь людям, которым трудно изучать химию по «плоским» картинкам и формулам. "
        "Визуализация в 3D (а также режимы VR и AR) делает абстрактные понятия — атомы, молекулы, элементы — "
        "наглядными и «осязаемыми»: объект можно покрутить, приблизить и рассмотреть под любым углом. "
        "Это повышает понимание, интерес и запоминание материала.",
    )
    add_bullets(
        doc,
        [
            "подходит для визуального обучения и снижает сложность восприятия темы;",
            "повышает вовлечённость за счёт интерактива и 3D;",
            "VR даёт эффект «погружения» в сцену, AR — «модель в комнате» через камеру телефона;",
            "позволяет учить материал на устройствах: ПК/смартфон/VR‑шлем (при наличии).",
        ],
    )

    add_h1(doc, "Цель и задачи")
    add_h2(doc, "Цель")
    add_p(
        doc,
        "Создать учебную VR/AR платформу по химии «Интерактивная 3D-химия», где пользователь изучает элементы "
        "и молекулы через 3D‑просмотр и может запускать VR и AR прямо со страницы.",
    )
    add_h2(doc, "Задачи")
    add_bullets(
        doc,
        [
            "сделать навигацию по страницам (главная → таблица → элемент → 3D‑просмотр);",
            "реализовать интерактивные 3D‑сцены и управление камерой (OrbitControls);",
            "добавить VR‑режим через WebXR (кнопка «Посмотреть в VR»);",
            "добавить AR‑режим через WebXR (кнопка «Смотреть в AR»);",
            "подготовить учебный контент простыми словами (описания, факты, примеры);",
            "реализовать 3D‑объекты для уроков (пример: молекула воды H₂O).",
        ],
    )

    add_h1(doc, "Что реализовано в проекте (по коду)")
    add_bullets(
        doc,
        [
            "Главная страница «Интерактивная 3D-химия» с описанием и переходом к таблице.",
            "Страница «Первые 10 элементов»: сетка и переход на карточку элемента.",
            "Страница «Все элементы Менделеева»: 118 элементов, поиск и фильтр по категориям.",
            "Карточка элемента: объяснения простыми словами + интерактивная 3D‑модель с кнопками VR/AR.",
            "Урок «Химия: Молекула воды»: 3D‑модель H₂O + VR/AR переключение.",
        ],
    )
    add_p(
        doc,
        "В проекте предусмотрен маршрут «сканирование карточки» (переход «Оживить карточку» для водорода), "
        "но сама страница сканирования (`src/pages/ScanPage.tsx`) сейчас пустая — сценарий AR‑сканирования "
        "пока не реализован.",
    )

    add_h1(doc, "Преимущества и отличия проекта")
    add_p(doc, "Ключевые преимущества:")
    add_bullets(
        doc,
        [
            "учебная направленность: тексты простыми словами + факты + примеры из жизни, а не просто 3D‑демо;",
            "одна и та же 3D‑сцена запускается как обычный просмотр и как VR/AR (через XR-обёртку);",
            "расширяемость: новые молекулы/объекты добавляются как компоненты в `src/components/`;",
            "покрытие контента: 118 элементов в таблице + расширенные карточки (первые 10) + 3D‑модели элементов.",
        ],
    )

    add_h1(doc, "Использованные технологии (фактически в проекте)")
    add_p(doc, "Проект реализован как веб‑приложение с 3D‑рендером и WebXR для VR/AR.")
    add_bullets(
        doc,
        [
            "React + TypeScript.",
            "react-router-dom (маршрутизация).",
            "Vite (сборка/запуск).",
            "Tailwind CSS + CSS Modules (стили).",
            "three (3D‑движок).",
            "@react-three/fiber (Three.js через React).",
            "@react-three/drei (OrbitControls и утилиты).",
            "@react-three/xr (WebXR VR/AR: `createXRStore()`, `enterVR()`, `enterAR()`).",
            "@splinetool/react-spline (3D‑сцена на главной странице).",
        ],
    )

    add_h1(doc, "Как реализованы AR и VR в проекте")
    add_h2(doc, "Общий принцип")
    add_p(
        doc,
        "3D‑сцена (объект + свет + управление камерой) находится внутри компонента `<XR store={xrStore}>`. "
        "На страницах создаётся `xrStore = createXRStore()`, а запуск режимов выполняется кнопками "
        "`xrStore.enterVR()` и `xrStore.enterAR()`.",
    )
    add_h2(doc, "VR")
    add_p(
        doc,
        "При нажатии «Посмотреть в VR» создаётся WebXR VR‑сессия. Браузер шлема переводит рендер в стереорежим, "
        "и пользователь видит ту же сцену как виртуальное пространство.",
    )
    add_h2(doc, "AR")
    add_p(
        doc,
        "При нажатии «Смотреть в AR» создаётся WebXR AR‑сессия. Пользователь смотрит через камеру телефона, "
        "а 3D‑модель отображается поверх реального мира. Совместимость зависит от браузера и устройства.",
    )

    add_h1(doc, "Как работает AR и VR (простыми словами)")
    add_h2(doc, "AR")
    add_p(
        doc,
        "AR показывает реальный мир через камеру и добавляет поверх него виртуальный объект. Поэтому пользователь "
        "видит свою комнату, а модель (например, молекула) «как будто стоит» перед ним.",
    )
    add_h2(doc, "VR")
    add_p(
        doc,
        "VR полностью переносит пользователя в виртуальную сцену: реальную комнату не видно. Шлем отслеживает "
        "движения головы и показывает картинку отдельно для каждого глаза, создавая ощущение присутствия.",
    )

    add_h1(doc, "Как запустить AR и VR, в том числе с VR‑очками")
    add_h2(doc, "VR (шлем)")
    add_bullets(
        doc,
        [
            "Откройте сайт в браузере VR‑шлема (в подсказках проекта упомянут Meta Quest Browser).",
            "Перейдите на страницу элемента или урока.",
            "Нажмите «Посмотреть в VR».",
            "Подтвердите запрос на вход в VR‑режим (если появится).",
        ],
    )
    add_h2(doc, "AR (телефон)")
    add_bullets(
        doc,
        [
            "Откройте страницу на смартфоне и разрешите доступ к камере (если браузер запросит).",
            "Перейдите на страницу элемента/урока и нажмите «Смотреть в AR».",
            "Если AR поддерживается, откроется режим камеры с наложенной 3D‑моделью.",
        ],
    )
    add_p(
        doc,
        "Примечание: поддержка WebXR AR зависит от устройства и браузера. В тексте подсказки проекта указано: "
        "Android — Chrome, iOS — Safari; на практике совместимость может отличаться.",
    )
    add_h2(doc, "Чек‑лист совместимости")
    add_bullets(
        doc,
        [
            "VR: нужен шлем с поддержкой WebXR в браузере.",
            "AR: нужен смартфон с камерой и поддержкой AR‑режима в браузере.",
            "3D: сцены сделаны на Three.js/React Three Fiber (геометрия и материалы в коде).",
        ],
    )

    return doc


def main() -> None:
    out_dir = Path(__file__).resolve().parents[1] / "docs"
    out_dir.mkdir(parents=True, exist_ok=True)
    out_path = out_dir / "VR_AR_platform_description.docx"

    doc = build_doc()
    doc.save(out_path)
    print(str(out_path))


if __name__ == "__main__":
    main()

